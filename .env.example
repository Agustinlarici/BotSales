# Connection string del Postgres (en Railway: la interna
# ${{Postgres.DATABASE_URL}}, ya que la app corre dentro del mismo proyecto).
DATABASE_URL=

# Contraseña compartida que protege toda la app (UI + API), ya que corre en
# una URL pública. Elegí cualquier valor largo y avisale a Claude cuál es.
APP_PASSWORD=
